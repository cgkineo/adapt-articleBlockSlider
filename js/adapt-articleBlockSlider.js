define([
	'coreJS/adapt',
	'./adapt-articleExtension'
], function(Adapt) {

	var blockSlider = _.extend({

		initialize: function() {
			//initialising the block slider extension
		}

	}, Backbone.Events);

	blockSlider.initialize();

});